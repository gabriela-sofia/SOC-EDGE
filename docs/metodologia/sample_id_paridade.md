# sample_id e paridade Python vs ESP32

O `sample_id` é um identificador único da amostra. Ele não é feature do modelo.

Ele evita merge por ordem de linha e permite detectar perda, duplicação, reset e parsing quebrado.
